# design/tokens/

색·타이포·간격·반경·그림자 등 design token 탐색 / 확정 문서.

- `colors.v1.md`, `typography.v1.md`, `spacing.v1.md` ...
- 버전은 파일명에 (`v1`, `v2`) — 덮어쓰지 않고 히스토리 유지.
- 확정본은 `packages/design-system/src/tokens.ts` 로 승격.
- `.github/workflows/design-token-sync.yml` 이 양쪽 값 일치를 CI 단계에서 검증.
