{
  "name": "hangul-route",
  "version": "0.0.0",
  "private": true,
  "description": "Hangul Route — Korean language learning app for heritage and K-culture kids (ages 5–11)",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
