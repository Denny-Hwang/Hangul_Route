{
  "name": "@hangul-route/content-schema",
  "version": "0.0.0",
  "private": true,
  "description": "Episode / Card / Grid / Quest schemas (zod) for Hangul Route content",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo '(no lint yet — @hangul-route/content-schema)'",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "build": "echo '(no build output yet — @hangul-route/content-schema)'"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@hangul-route/test-utils": "workspace:*",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
