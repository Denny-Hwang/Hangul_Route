{
  "name": "@hangul-route/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Types shared between backend and frontend (no runtime deps)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo '(no lint yet — @hangul-route/shared-types)'",
    "test": "echo '(no tests yet — @hangul-route/shared-types)'",
    "typecheck": "tsc --noEmit",
    "build": "echo '(no build output yet — @hangul-route/shared-types)'"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
